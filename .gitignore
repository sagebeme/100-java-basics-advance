# Compiled class files
*.class

# Build tool output (if a day adds Maven/Gradle later)
target/
build/
out/

# IDE
.idea/
*.iml
.vscode/
.classpath
.project
.settings/

# OS
.DS_Store
Thumbs.db

# Packaged artifacts
*.jar
*.war

# ...except the JUnit runner checked into lib/, so `git clone` is enough to run tests
!lib/*.jar
